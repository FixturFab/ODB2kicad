#pragma once
#include "string.h"
#include <cstdlib>
#include <cstdio>

// wxAtoi, wxAtol, wxAtof are already defined in string.h
// wxRemove, wxRename, wxFopen are in filefn.h (included transitively via string.h)
